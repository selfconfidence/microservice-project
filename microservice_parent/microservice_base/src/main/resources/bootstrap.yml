spring:
  cloud:
    config:
      name: base   # gitee中文件名前缀
      profile: dev # gitee中文件名后缀 所以名称规范才是 ${name}-${end}.yml
      label: master # 那个分支
      uri: http://192.168.238.166:12380 # config的服务端
      
      
